# Per-cluster overlay for controllers — cluster-specific patches and
# customizations for kube1.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../common
# Add per-cluster patches here:
# patches:
#   - path: patches/<name>.yaml
