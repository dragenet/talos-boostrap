# Generated by render-config; do not edit.
# Placeholder — the render compiler will populate this ConfigMap with cluster
# identity and feature configuration derived from config/overrides/cluster.yaml.
# Intended for consumption by other infrastructure components (e.g. dashboards,
# admission webhooks) that need cluster metadata at runtime.
#
# Not yet referenced in base/kustomization.yaml resources: — will be wired up
# when the render step is implemented.
apiVersion: v1
kind: ConfigMap
metadata:
  name: cluster-vars
  namespace: flux-system
data: {}
