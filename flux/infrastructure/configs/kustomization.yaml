# User-owned overlay — references the per-cluster config overlay chain.
# See ADR-017 for the four-tier overlay ownership model.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - overlays/clusters/kube1
