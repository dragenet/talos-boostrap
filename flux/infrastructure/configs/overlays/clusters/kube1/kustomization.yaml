# Per-cluster overlay for configs — cluster-specific config overrides for kube1.
# This is the per-cluster overlay layer (ADR-017).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../common
