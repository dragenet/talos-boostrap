# Repo-wide overlay for configs — repo/user preferences applied to every cluster.
# This is the common/user-global overlay layer (ADR-017).
# Add patches or replacements here for shared customizations.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
