---
# This inventory carries only group_vars — no hosts are declared here.
# Provider-specific inventories (hcloud/, manual/) declare the actual hosts.
# Ansible requires at least one parseable file in each -i directory.
all: {}
